apiVersion: v1
kind: Service
metadata:
  name: {{ include "loom-masterdata-configuration.fullname" . }}
  labels:
    {{- include "loom-masterdata-configuration.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.restPort }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "loom-masterdata-configuration.selectorLabels" . | nindent 4 }}

