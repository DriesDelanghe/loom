{{- if .Values.frontend.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "loom-masterdata-configuration.fullname" . }}-frontend
  labels:
    {{- include "loom-masterdata-configuration.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    {{- include "loom-masterdata-configuration.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
{{- end }}


