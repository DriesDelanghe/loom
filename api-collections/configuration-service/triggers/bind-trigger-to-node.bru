meta {
  name: Bind Trigger to Entry Node
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/triggers/bindings/nodes
  body: json
}

body:json {
  {
    "triggerBindingId": "{{bindingId}}",
    "entryNodeId": "{{nodeId}}",
    "order": 0
  }
}

