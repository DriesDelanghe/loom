meta {
  name: Update Trigger Config
  type: http
  seq: 2
}

put {
  url: {{baseUrl}}/api/triggers/{{triggerId}}/config
  body: json
}

body:json {
  {
    "config": {
      "key": "value"
    }
  }
}

