# API Configuration
# Base URL for the configuration service API
# Default: /api (relative, proxied through nginx)
VITE_API_BASE_URL=/api

# Layout Service API URL
# Default: /api/layout (relative, proxied through nginx)
# For local development with separate service: http://localhost:8081/api
VITE_LAYOUT_API_URL=/api/layout
